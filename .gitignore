*.tmp
.DS_Store

# the reference shelf. published books, hardlinked in from ~/Downloads --
# readable locally at /refs/, never committed and never pushed.
refs/*.pdf

# uv's virtual environment. Rebuilt by `uv sync` from pyproject.toml and
# uv.lock, both of which are committed.
.venv/
