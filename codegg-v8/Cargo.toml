[package]
name = "eggv8"
version = "0.1.0"
edition = "2021"

# std only, deliberately: the transmuter is number theory, a match finder and
# a walk down the dyadic tree, and the build must work with no network and no
# supply chain.
[dependencies]

[profile.release]
opt-level = 3
lto = true
